数组是存放在连续内存空间上的相同类型数据的集合

数组下表都是从0开始的。
数组内存空间的地址是连续的
正是「因为数组的在内存空间的地址是连续的，所以我们在删除或者增添元素的时候，就难免要移动其他元素的地址。

数组的元素是不能删的，只能覆盖。

二维数据在内存中不是 3*4 的连续地址空间，而是四条连续的地址空间组成！

数组解题思想：二分法、双指针法、滑动窗口

滑动窗口代码模板
int left = 0, right = 0;

while (right < s.size()) {`
    // 增大窗口
    window.add(s[right]);
    right++;

    while (window needs shrink) {
        // 缩小窗口
        window.remove(s[left]);
        left++;
    }
}